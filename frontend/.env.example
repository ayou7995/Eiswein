# Base URL for the Eiswein API. In dev this is proxied by Vite (see vite.config.ts),
# in prod the frontend is served by the same origin as the API so this can be empty.
VITE_API_URL=http://localhost:8000
